# Application
APP_NAME=EDF gRPC Viewer
DEBUG=False
HOST=127.0.0.1
PORT=8000

# CORS — comma-separated origins (use * for dev)
CORS_ORIGINS=http://localhost:3000

# gRPC server
GRPC_HOST=0.0.0.0
GRPC_PORT=50051
GRPC_MAX_WORKERS=10
GRPC_MAX_MESSAGE_MB=50

# EDF data source (relative to backend dir, or absolute)
EDF_FILE_PATH=aaaaamrj_s001_t000.edf